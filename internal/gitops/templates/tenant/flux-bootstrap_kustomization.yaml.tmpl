apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-bootstrap-{{.Name}}
  namespace: vcluster-{{.Name}}
spec:
  interval: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  path: ./clusters/{{.Env}}/vclusters/{{.Name}}/tenant/flux-bootstrap
  prune: true
  timeout: 5m
